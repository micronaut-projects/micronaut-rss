plugins {
    id 'io.micronaut.build.internal.rss-base'
}

// Shared setup of the test-suite* projects backing the snippet:: macros of the guide.
repositories {
    mavenCentral()
}

tasks.withType(Test).configureEach {
    useJUnitPlatform()
}
